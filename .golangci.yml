# This file configures github.com/golangci/golangci-lint.
version: "2"

linters:
  exclusions:
    paths:
      - tests
      - consensus/polybft
      - e2e-polybft
      - command/polybftsecrets
      - command/rootchain
      - state/runtime/precompiled/elGamal*
      - helper/elgamal/twistedelgamal.go
    rules:
      - path: _test\.go
        linters:
          - gosec
          - unparam
          - lll
          - forcetypeassert
          - errcheck
      - path: gen_sc_data\.go
        linters:
          - wsl
          - lll
          - stylecheck
    presets:
      - comments
  default: none
  enable:
    - whitespace # Tool for detection of leading and trailing whitespace
    - wsl_v5 # Forces you to use empty lines
    - wastedassign # Finds wasted assignment statements
    - unconvert # Unnecessary type conversions
    - tparallel # Detects inappropriate usage of t.Parallel() method in your Go test codes
    - thelper # Detects golang test helpers without t.Helper() call and checks the consistency of test helpers
    - prealloc # Finds slice declarations that could potentially be pre-allocated
    - predeclared # Finds code that shadows one of Go's predeclared identifiers
    - nolintlint # Ill-formed or insufficient nolint directives
    - nlreturn # Checks for a new line before return and branch statements to increase code clarity
    - misspell # Misspelled English words in comments
    - makezero # Finds slice declarations with non-zero initial length
    - lll # Long lines
    - importas # Enforces consistent import aliases
    - gosec # Security problems
    - govet # Whether the code was govet-ed
    - goconst # Repeated strings that could be replaced by a constant
    - forcetypeassert # Finds forced type assertions
    - dogsled # Checks assignments with too many blank identifiers (e.g. x, , , _, := f())
    - dupl # Code clone detection
    - errname # Checks that sentinel errors are prefixed with the Err and error types are suffixed with the Error
    - errorlint # errorlint is a linter for that can be used to find code that will cause problems with the error wrapping scheme introduced in Go 1.13
    - gocritic
    - errcheck # Errcheck is a go lint rule for checking for unchecked errors in go programs. These unchecked errors can be critical bugs in some cases
    - godox # Godox is a linter for TODOs and FIXMEs left in the code

  settings:
    goconst:
      min-len: 3
      min-occurrences: 3
    gocritic:
      enabled-checks:
        - ruleguard
      settings:
        ruleguard:
          rules: "./gorules/rules.go"
    gosec:
      excludes:
        - G104 # Errors unhandled
        - G115 # Potential integer overflow when converting between integer types
        - G304 # File path provided as taint input to a method that can trigger path traversal vulnerabilities
        - G705 # XSS via taint analysis
